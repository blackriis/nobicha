# 📊 คู่มือ Import ข้อมูลวัตถุดิบผ่าน CSV

## 🎯 ขั้นตอนการ Import (ใช้เวลาประมาณ 5-10 นาที)

### 1️⃣ เตรียมไฟล์ CSV

#### ใช้ Template ที่เตรียมไว้ให้:
ไฟล์: `raw_materials_import_template.csv`

#### หรือสร้างเองใน Excel/Google Sheets:

**Headers (แถวแรก - บังคับ):**
```
name,unit,cost_per_unit,supplier,description,is_active
```

**ตัวอย่างข้อมูล:**
```csv
name,unit,cost_per_unit,supplier,description,is_active
แป้งสาลี,กก.,50.00,บริษัท อาหารใส,แป้งสาลีคุณภาพสูง,true
น้ำตาล,กก.,35.00,บริษัท หวานมาก,น้ำตาลทรายขาว,true
นม,ลิตร,45.00,ฟาร์มนมสด,นมสดพาสเจอร์ไรส์,true
```

---

### 2️⃣ กฎการกรอกข้อมูล

| คอลัมน์ | บังคับ | รูปแบบ | ตัวอย่าง |
|---------|--------|--------|----------|
| **name** | ✅ | ข้อความ ไม่เกิน 255 ตัวอักษร | `แป้งสาลี`, `น้ำตาล` |
| **unit** | ✅ | ข้อความ ไม่เกิน 50 ตัวอักษร | `กก.`, `ลิตร`, `ชิ้น`, `ฟอง` |
| **cost_per_unit** | ✅ | ตัวเลข ทศนิยม 2 ตำแหน่ง | `50.00`, `35.50`, `120.75` |
| **supplier** | ❌ | ข้อความ ไม่เกิน 255 ตัวอักษร | `บริษัท อาหารใส` |
| **description** | ❌ | ข้อความ | `แป้งสาลีคุณภาพสูง` |
| **is_active** | ❌ | `true` หรือ `false` | `true` (default) |

**หมายเหตุ:**
- ✅ = บังคับต้องกรอก
- ❌ = ไม่บังคับ (ปล่อยว่างได้)
- ห้ามมีช่องว่างใน header names
- ถ้าไม่กรอก `is_active` จะเป็น `true` อัตโนมัติ

---

### 3️⃣ Import ผ่าน Supabase Dashboard

#### ขั้นตอนที่ 1: เข้าสู่ Supabase
1. ไปที่ https://supabase.com/dashboard
2. Login เข้าบัญชี
3. เลือก **Production Project** ของคุณ

#### ขั้นตอนที่ 2: ไปที่ Table Editor
1. คลิก **Table Editor** ที่แถบด้านซ้าย
2. เลือกตาราง **raw_materials**

#### ขั้นตอนที่ 3: Import CSV
1. คลิกปุ่ม **"Insert"** ด้านบน
2. เลือก **"Import data from CSV"**
3. คลิก **"Choose File"** และเลือกไฟล์ CSV ของคุณ
4. ตรวจสอบ Preview ว่าข้อมูลถูกต้อง
5. คลิก **"Import"**

#### ขั้นตอนที่ 4: ตรวจสอบผลลัพธ์
- Supabase จะแสดงข้อความว่า Import สำเร็จ
- ตรวจสอบว่าข้อมูลปรากฏในตาราง
- ตรวจนับจำนวนแถวว่าครบหรือไม่

---

### 4️⃣ ตรวจสอบข้อมูลที่ Import

#### ผ่าน Supabase Table Editor:
- ดูข้อมูลใน Table Editor ได้ทันที
- สามารถแก้ไขข้อมูลได้ทีละแถว

#### ผ่าน SQL Editor (ตรวจสอบเพิ่มเติม):
```sql
-- ดูข้อมูลทั้งหมด
SELECT * FROM raw_materials ORDER BY created_at DESC;

-- นับจำนวน
SELECT COUNT(*) as total_materials FROM raw_materials;

-- ดูเฉพาะที่ active
SELECT name, unit, cost_per_unit, supplier
FROM raw_materials
WHERE is_active = true
ORDER BY name;
```

---

## ⚠️ ปัญหาที่อาจพบและวิธีแก้

### ❌ "Column 'name' cannot be null"
**สาเหตุ:** ไม่ได้กรอกชื่อวัตถุดิบ
**วิธีแก้:** ตรวจสอบว่าทุกแถวมีค่าในคอลัมน์ `name`

### ❌ "Invalid number format"
**สาเหตุ:** `cost_per_unit` ไม่ใช่ตัวเลข
**วิธีแก้:**
- ใช้เฉพาะตัวเลขและจุดทศนิยม เช่น `50.00`
- ห้ามใส่เครื่องหมาย `฿` หรือ `,` (comma)

### ❌ "Header mismatch"
**สาเหตุ:** ชื่อ header ไม่ตรงกับ database
**วิธีแก้:** ใช้ header ตาม template ที่ให้มา (copy-paste)

### ❌ "CSV encoding error" (ตัวอักษรไทยแสดงผิด)
**วิธีแก้:**
1. เปิดไฟล์ใน Notepad/TextEdit
2. Save As → เลือก encoding: **UTF-8**
3. Import ใหม่อีกครั้ง

---

## 💡 เทคนิคและ Tips

### ✅ สำหรับข้อมูลจำนวนมาก (100+ แถว):
- แบ่ง Import ทีละ 100-200 แถว
- ตรวจสอบผลลัพธ์ก่อน Import รอบถัดไป

### ✅ การ Update ข้อมูลที่มีอยู่แล้ว:
- Supabase CSV Import จะ **สร้างแถวใหม่เสมอ** (ไม่ update)
- ถ้าต้องการ update: ใช้ SQL Script แทน

### ✅ Backup ก่อน Import ข้อมูลจำนวนมาก:
```sql
-- Export ข้อมูลเดิมก่อน (รันใน SQL Editor)
SELECT * FROM raw_materials;
-- Copy ผลลัพธ์ไว้เผื่อต้อง restore
```

### ✅ Template Excel/Google Sheets:
หากต้องการใช้ Excel:
1. กรอกข้อมูลตาม template
2. File → Save As → เลือกไฟล์ประเภท **CSV UTF-8 (Comma delimited)**
3. บันทึกและ Import

---

## 🔄 ทางเลือกอื่น: ใช้ SQL Script แทน

หากมีปัญหากับ CSV Import สามารถใช้ SQL Script ได้:

```sql
INSERT INTO raw_materials (name, unit, cost_per_unit, supplier, description, is_active)
VALUES
  ('แป้งสาลี', 'กก.', 50.00, 'บริษัท อาหารใส', 'แป้งสาลีคุณภาพสูง', true),
  ('น้ำตาล', 'กก.', 35.00, 'บริษัท หวานมาก', 'น้ำตาลทรายขาว', true),
  ('นม', 'ลิตร', 45.00, 'ฟาร์มนมสด', 'นมสดพาสเจอร์ไรส์', true);
```

รันใน SQL Editor บน Supabase Dashboard

---

## 📞 ต้องการความช่วยเหลือเพิ่มเติม?

- ปัญหาการ Import: ดู error message แล้วเช็คกับตาราง "ปัญหาที่อพจพบ"
- ต้องการ Import ข้อมูลจำนวนมาก (1000+ แถว): แนะนำใช้ Node.js script
- ต้องการ validation ข้อมูลก่อน import: ใช้ SQL Script พร้อม checks

---

## ✅ Checklist หลัง Import สำเร็จ

- [ ] ตรวจนับจำนวนแถวว่าตรงกับที่ import
- [ ] ตรวจสอบว่า `cost_per_unit` ถูกต้อง
- [ ] ตรวจสอบว่าตัวอักษรไทยแสดงผลถูกต้อง
- [ ] ลอง query ข้อมูลผ่าน SQL Editor
- [ ] ทดสอบใช้งานใน Application (Material Usage feature)

---

**สร้างโดย Claude Code** 🤖
*Last updated: 2025-11-30*
